branches:
  only:
    - master

language: java

jdk:
  - oraclejdk9
  - oraclejdk11
  - openjdk9
  - openjdk11

before_install:
  - mvn license:format --quiet -B

after_success:
  - mvn clean test jacoco:report coveralls:report
